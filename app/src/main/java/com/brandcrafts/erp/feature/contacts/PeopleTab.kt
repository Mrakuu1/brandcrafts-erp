package com.brandcrafts.erp.feature.contacts

/** Presentation-only selector; Contact and Employee data remain in their own modules. */
enum class PeopleTab {
    CUSTOMERS,
    SUPPLIERS,
    EMPLOYEES,
}
